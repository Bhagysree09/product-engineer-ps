{
  "name": "reliable-conversation-runtime",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "bench": "tsx src/bench.ts",
    "demo": "tsx src/cli.ts",
    "verify": "npm run typecheck && npm test && npm run bench"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "tsx": "^4.23.15",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
